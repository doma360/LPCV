# Optionnel. Sans ça, l'app déduit l'IP de la machine qui fait tourner
# `expo start` à partir du packager Metro (fonctionne sur simulateur/Expo Go).
# Utile surtout si l'API tourne ailleurs qu'en local (préprod...).
# EXPO_PUBLIC_API_URL="http://192.168.1.10:4000"
